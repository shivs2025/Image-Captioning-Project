# Image-Captioning-Project
A deep learning project to generate image captions using CNN + LSTM

This branch extracts features from the Flickr8k images using the InceptionV3 model and saves them as a .npy file.
